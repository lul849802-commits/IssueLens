import Link from "next/link";
export function AnalysisHeader({ runId, repository, active, readOnly }: { runId: string; repository: string; active: "progress" | "overview"; readOnly?: boolean }) { return <header className="analysis-header"><Link className="brand" href="/">IssueLens</Link><div className="repo-context"><strong>{repository}</strong>{readOnly && <span className="badge neutral">只读分享</span>}</div><nav aria-label="分析导航"><a className={active === "progress" ? "active" : ""} href={`/analysis/${runId}/progress`}>任务状态</a><a className={active === "overview" ? "active" : ""} href={`/analysis/${runId}/overview`}>洞察总览</a></nav></header>; }
